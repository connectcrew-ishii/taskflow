services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - taskflow_data:/app/data
    environment:
      - DATABASE_URL=sqlite:////app/data/taskflow.db

volumes:
  taskflow_data: